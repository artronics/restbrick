server:
  port: 8080
  address: 127.0.0.1

spring:
  jpa:
    database: MYSQL
    hibernate:
      ddl-auto: create-drop
  datasource:
    url: jdbc:mysql://localhost:3306/brickstore?autoReconnect=true&useSSL=false
    username: root
    password: 6220617

spring.jackson.serialization.INDENT_OUTPUT: true

logging.level:
  com.artronics: INFO
  org.springframework: ERROR
  org.elasticsearch: ERROR
  org.hibernate: ERROR
  io.undertow: ERROR
  org.xnio: ERROR
  org.apache: ERROR
  org.hibernate.SQL: DEBUG
  org.hibernate.type.descriptor.sql.BasicBinder: TRACE

#management.port: -1

#spring:
#  datasource:
#    platform: h2
#    url: "jdbc:h2:mem:"

spring.jpa.hibernate.ddl-auto: create-drop
